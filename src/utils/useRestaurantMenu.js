import { MENU_CATEGORIES } from "../utils/mockMenu";

const useRestaurantMenu = () => {
  // Returns the array of 10 categories containing 100 items total
  return MENU_CATEGORIES;
};

export default useRestaurantMenu;
